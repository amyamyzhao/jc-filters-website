# Project maintenance constraints

## Locked approved footer

- The current site footer is approved and locked as of 2026-09-23.
- Do not change the footer structure, content, styling, spacing, colors, navigation, contact details, responsive layout, or related CSS.
- This lock applies to `Footer` in `app/components.tsx` and all `.site-footer`, `.footer-*`, and `.footer-wa` rules in `app/globals.css`.
- Only modify the footer after the user explicitly says to unlock it or explicitly requests a specific footer change.
